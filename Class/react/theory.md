# *React* 
- its a library developed by meta to build a fast and interactive user interfaces so by using js 
- it is a single page application, page does not reload only the changes are made 
- ex - insta

## *Why we use react?*
- Probs with js:   
    - full page reload   
    - complex dom mainpulation  
    - hard to manage large application
- Updates only required part, uses reuable components

jsx is used to write javascript + html

rules must return single parent 
use curle bracket for 

use component kasa karnar

function app() {return <>
<student/>
}

probs means data pass through parent to child

function app() {
    return <student name='King'/>
}
function student(probs){
    return <h2>name:{probs.name}</h2>
}

activity: what is componentstypes of comp where we use comp in react with two examples
what is probs why we use probs types of probs with two examples
